# mppx déclare Express >= 5 en peer optionnelle ; l'application est en Express 4
# et n'utilise pas son adaptateur Express (pont maison dans src/lib/mpp.js).
# Sans cette ligne, `npm install` échoue au build.
legacy-peer-deps=true
